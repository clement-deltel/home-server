# https://github.com/bastienwirtz/homer
services:
  homer:
    container_name: homer
    dns:
      - 127.0.0.1
      - ${DNS_SERVER_1}
      - ${DNS_SERVER_2}
    env_file: config/.env
    hostname: homer
    image: ${HOMER_IMAGE}
    labels:
      - "traefik.enable=true"
      # Routers
      - "traefik.http.routers.homer.entrypoints=web-external,websecure-external"
      - "traefik.http.routers.homer.rule=Host(`dashboard.${DOMAIN}`)"
      - "traefik.http.routers.homer.tls=true"
      # - "traefik.http.routers.homer.tls.certresolver=letsencrypt"
      # - "traefik.http.routers.homer.tls.domains[0].main=dashboard.${DOMAIN}"
      - "traefik.http.routers.homer.middlewares=homer"
      - "traefik.http.routers.homer.service=homer"
      # Middlewares
      - "traefik.http.middlewares.homer.basicauth.users=${HOMER_USER}:${HOMER_PASSWORD}"
      # Services
      - "traefik.http.services.homer.loadbalancer.server.port=8080"
    logging:
      driver: json-file
      options:
        max-file: 5
        max-size: 10m
    networks:
      proxy-net:
        ipv4_address: ${HOMER_IP}
    ports:
      - 8080
    restart: unless-stopped
    stop_grace_period: 30s
    user: ${DOCKER_UID}:${DOCKER_GID}
    volumes:
      - ./persistence:/www/assets

networks:
  proxy-net:
    external: true
    name: traefik-net
